# NetProbe Server — Python dependencies
# Install: pip install -r requirements.txt

# ── ASGI framework + server ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # includes websockets, httptools, uvloop (Linux)
websockets>=12.0

# ── network engine deps ───────────────────────────────────────────────────────
scapy>=2.5.0                   # ARP scan, packet capture
dnspython>=2.6.0               # Full DNS record types
psutil>=5.9.0                  # Netstat process names, interface list
cryptography>=42.0.0           # Richer SSL cert parsing (optional fallback)

# ── packaging (only needed when building NetProbeServer.exe) ─────────────────
# pyinstaller>=6.0.0
